
\chapter{CRR --- Component Responsibility Report}

El CRR (\textit{Component Responsibility Report}) es un documento que consolida, para cada componente significativo del sistema, sus responsabilidades, dependencias, puntos de falla y criterios de calidad. Funciona como un puente entre las CRC Cards (orientadas a clases del dominio) y la documentación arquitectónica C4 (orientada a contenedores y componentes).

Para cada componente principal se documenta: descripción, responsabilidades clave, entradas y salidas, dependencias, riesgos y atributos de calidad relevantes.

\newcommand{\crrrow}[2]{%
  \textbf{\color{fmAccent}#1} & #2 \\
}

\section{CRR --- Aplicación Web SPA}

\begin{tabularx}{\textwidth}{>{\bfseries}L{3.5cm} X}
  \toprule
  \rowcolor{fmTableHeader}
  \textbf{\color{fmAccent}Atributo} & \textbf{\color{fmAccent}Detalle} \\
  \midrule
  \rowcolor{fmTableAltRow}
  Tecnología & React 19 + TypeScript + Vite (PWA) + Zustand + React Router + Tailwind CSS + Socket.IO client \\
  Responsabilidades & Renderizar UI responsiva para los tres roles; gestionar estado y sesión con Zustand; capturar audio con MediaRecorder; consumir la API por REST y recibir eventos en tiempo real por WebSocket; soportar i18n, modo claro/oscuro y uso offline (service worker). \\
  \rowcolor{fmTableAltRow}
  Entradas & Interacción del usuario, respuestas REST, eventos de Socket.IO, audio del micrófono. \\
  Salidas & Peticiones HTTP/JSON, audio multipart, mensajes por WebSocket. \\
  \rowcolor{fmTableAltRow}
  Dependencias & API REST + WebSocket; navegador con soporte de MediaRecorder. \\
  Riesgos & Compatibilidad de captura de audio en Safari iOS; fallos de red (mitigado con reintentos, caché offline y cola de feedback diferido). \\
  \rowcolor{fmTableAltRow}
  Atributos de calidad & Usabilidad (RNF-050, RNF-051), Compatibilidad (RNF-070). \\
  \bottomrule
\end{tabularx}

\section{CRR --- Reverse-proxy / TLS}

\begin{tabularx}{\textwidth}{>{\bfseries}L{3.5cm} X}
  \toprule
  \rowcolor{fmTableHeader}
  \textbf{\color{fmAccent}Atributo} & \textbf{\color{fmAccent}Detalle} \\
  \midrule
  \rowcolor{fmTableAltRow}
  Tecnología & nginx (sirve la SPA) y Caddy (HTTPS automático en producción) \\
  Responsabilidades & Terminar TLS; servir los estáticos de la SPA; enrutar \texttt{/api}, \texttt{/uploads} y \texttt{/socket.io} hacia la API (mismo origen, sin CORS). \\
  \rowcolor{fmTableAltRow}
  Entradas & Peticiones HTTPS de los navegadores. \\
  Salidas & Estáticos de la SPA; proxy a la API y al canal WebSocket. \\
  \rowcolor{fmTableAltRow}
  Dependencias & API REST + WebSocket; Let's Encrypt (certificados). \\
  Riesgos & Configuración del upgrade de WebSocket; renovación de certificados (automática con Caddy). \\
  \rowcolor{fmTableAltRow}
  Atributos de calidad & Seguridad (RNF-020), Disponibilidad (RNF-040). \\
  \bottomrule
\end{tabularx}

\section{CRR --- API REST + WebSocket}

\begin{tabularx}{\textwidth}{>{\bfseries}L{3.5cm} X}
  \toprule
  \rowcolor{fmTableHeader}
  \textbf{\color{fmAccent}Atributo} & \textbf{\color{fmAccent}Detalle} \\
  \midrule
  \rowcolor{fmTableAltRow}
  Tecnología & Node.js + Express + Socket.IO + Prisma + Zod + Swagger/OpenAPI \\
  Responsabilidades & Monolito modular (rutas → controladores → servicios → repositorios). Autenticar (JWT con refresh), autorizar por rol, validar con Zod, aplicar rate limiting, registrar auditoría, exponer la API REST y el canal en tiempo real. Concentra los módulos de pacientes, clínico, rutinas, sesiones, feedback, agenda, mensajería y notificaciones. \\
  \rowcolor{fmTableAltRow}
  Entradas & Peticiones HTTPS/JSON y multipart desde la SPA; handshakes de Socket.IO con JWT. \\
  Salidas & Respuestas JSON; eventos en vivo (\texttt{message:new}, \texttt{notification:new}); correos por SMTP. \\
  \rowcolor{fmTableAltRow}
  Dependencias & PostgreSQL (Prisma), Redis (adapter Socket.IO), MinIO/S3, Whisper, Ollama, SMTP. \\
  Riesgos & Punto único de despliegue (mitigado con réplicas + adapter de Redis); latencia de la IA (mitigada ejecutándola fuera del camino crítico). \\
  \rowcolor{fmTableAltRow}
  Atributos de calidad & Seguridad (RNF-020, RNF-023, RNF-026), Auditoría (RNF-024), Mantenibilidad (RNF-061, RNF-063), Disponibilidad (RNF-040). \\
  \bottomrule
\end{tabularx}

\section{CRR --- Transcripción de voz (Whisper)}

\begin{tabularx}{\textwidth}{>{\bfseries}L{3.5cm} X}
  \toprule
  \rowcolor{fmTableHeader}
  \textbf{\color{fmAccent}Atributo} & \textbf{\color{fmAccent}Detalle} \\
  \midrule
  \rowcolor{fmTableAltRow}
  Tecnología & Python + Whisper (CPU), invocado como subproceso desde la API \\
  Responsabilidades & Transcribir a texto el audio del feedback del paciente. \\
  \rowcolor{fmTableAltRow}
  Entradas & Archivo de audio guardado por la API. \\
  Salidas & Transcripción en texto. \\
  \rowcolor{fmTableAltRow}
  Dependencias & ffmpeg; modelo Whisper precargado en la imagen. \\
  Riesgos & Costo de CPU de la inferencia (modelo \texttt{tiny} para acotarlo). \\
  \rowcolor{fmTableAltRow}
  Atributos de calidad & Rendimiento (RNF-003), Independencia de proveedor de IA (RNF-072). \\
  \bottomrule
\end{tabularx}

El resumen clínico del feedback lo genera \textbf{Ollama} (LLM local) como sistema externo; el módulo de notificaciones de la API envía correos por \textbf{SMTP} y empuja avisos en vivo por \textbf{Socket.IO} (con Redis para varias instancias). La persistencia es \textbf{PostgreSQL} (Prisma), el object storage \textbf{MinIO/S3} para avatares y documentos, y los respaldos se generan con \texttt{pg\_dump} programado.